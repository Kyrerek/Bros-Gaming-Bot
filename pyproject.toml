[project]
name = "brosgamingbot"
version = "0.1.0"
description = "A bot to suggest new games to play with bros"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["discord.py", 
    "python-dotenv", 
    "requests", 
    "psycopg[binary,pool]"
    ]
